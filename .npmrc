# Next.js + OpenNext 의 file trace 가 pnpm isolated linker 에서 indirect deps
# (@swc/helpers, styled-jsx, @next/env 등) 의 실제 파일을 못 따라가는 문제 회피.
# 모든 deps 를 root node_modules 에 flat 으로 hoist (npm 풍).
# 모노레포 전체 영향 — dev 환경 한정으로 진행. 운영 빌드 검증 끝나면 isolated 로 복귀 검토.
node-linker=hoisted
